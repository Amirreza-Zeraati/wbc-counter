def detect_and_count(image):
    """
    Detect and count white blood cells from processed image

    Args:
        image: preprocessed image (numpy array)

    Returns:
        tuple: (annotated_image, count)
    """
    count = 0
    return image, count
